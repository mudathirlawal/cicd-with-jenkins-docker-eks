    apiVersion: v1
    kind: Service
    metadata:
      name: web-app:v1.0
      labels:
      app: web-app:v1.0
    spec:
      type: LoadBalancer
      selector:
      app: 
        ports:
        - port: 80
            targetPort: 80
            protocol: TCP
     